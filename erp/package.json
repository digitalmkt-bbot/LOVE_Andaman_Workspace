{
  "name": "erp-api",
  "private": true,
  "type": "commonjs",
  "description": "ERP backend (Express over operation_schemas) + serves the built Vue SPA",
  "scripts": {
    "build": "npm --prefix web install && npm --prefix web run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.13.1"
  }
}
