# ============================================================
# Database connection for the STRUCTURED schema (operation_schemas)
# Do NOT commit real credentials. Rotate any password shared in chat.
# ============================================================

# --- Target database (choose the one your app should use) ---
# New DB #1 (reseau)
DATABASE_URL="postgresql://postgres:<PASSWORD>@reseau.proxy.rlwy.net:15797/railway"
# New DB #2 (thomas)
# DATABASE_URL="postgresql://postgres:<PASSWORD>@thomas.proxy.rlwy.net:26002/railway"

# The structured tables live in this schema (NOT public):
DB_SCHEMA="operation_schemas"

# Make unqualified queries resolve to it (Postgres search_path).
# Either set per-connection:  SET search_path TO operation_schemas, public;
# or configure your client/pool to run that on connect.

# --- adapter config (added) ---
DATA_BACKEND="blob"      # blob (default) | relational
PORT="3000"
# APP_STATE_ID="main"    # id of the legacy public.app_state row (blob backend)
