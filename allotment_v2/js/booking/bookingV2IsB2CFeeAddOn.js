// §b2cFee · จริงไหมว่ารายการนี้เป็นค่าธรรมเนียมจาก B2C (ไม่ใช่ของที่ต้องเตรียม)
//   ใช้ที่เดียวทุกหน้า · ถ้าวันหนึ่ง B2C เปลี่ยนรหัส แก้ตรงนี้จุดเดียว
function bookingV2IsB2CFeeAddOn(a){
  return !!(a && /^b2c-ad-/i.test(String(a.type||'')));
}
