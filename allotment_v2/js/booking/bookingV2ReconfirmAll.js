function bookingV2ReconfirmAll(date, rid, via){ if(!confirm('Mark ALL un-confirmed bookings on this trip as re-confirmed ('+(via==='phone'?'phone':'list')+')?')) return; (SB_BOOKINGS||[]).forEach(b=>{ if(['cancelled','rejected','cancelled_weather'].includes(b.status))return; if(b.ops&&b.ops.reconfirm&&b.ops.reconfirm.status==='done')return; if(!(b.trips||[]).some(t=>(t.date||'')===date && t.routeId===rid))return; b.ops=b.ops||{}; b.ops.reconfirm={status:'done',via:via||'list',at:new Date().toISOString(),by:laBy()}; if(typeof bookingV2AddHistory==='function') bookingV2AddHistory(b,'notify','Re-confirmed pickup ('+(via==='phone'?'phone':'list')+') · bulk','Notify'); }); acctPersistBookings(); if(typeof bookingV2Render==='function') bookingV2Render(); }
