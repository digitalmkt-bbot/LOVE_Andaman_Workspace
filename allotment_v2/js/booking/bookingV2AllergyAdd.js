function bookingV2AllergyAdd(){ const sm=_bkV2EnsureSM(); if(!sm) return; const ne=document.getElementById('bkv2-allerg-name'); const qe=document.getElementById('bkv2-allerg-qty'); const name=((ne&&ne.value)||'').trim(); if(!name) return; const qty=Math.max(1,Math.floor(Number(qe&&qe.value)||1)); const ex=sm.allergyList.find(a=>(a.name||'').toLowerCase()===name.toLowerCase()); if(ex) ex.qty=Math.floor(+ex.qty||0)+qty; else sm.allergyList.push({name,qty}); bookingV2Render(); }
